# Supabase Configuration
# Get these values from your Supabase project settings (https://app.supabase.com)
SUPABASE_URL=your_supabase_project_url
SUPABASE_KEY=your_supabase_service_role_key

# Default Password for Seeding Users (LOCAL DEVELOPMENT ONLY)
# This is only used when seeding default users (admin, operator, user)
# DO NOT set this in production/Vercel - create users manually via the registration API
DEFAULT_ADMIN_PASSWORD=your_secure_password_here

# Node Environment
NODE_ENV=development

# Notes:
# 1. For local development: Copy this file to server/.env and fill in your actual values
# 2. For Vercel deployment: Set SUPABASE_URL and SUPABASE_KEY in Vercel project settings
#    - Go to your Vercel project > Settings > Environment Variables
#    - Add SUPABASE_URL and SUPABASE_KEY
#    - DO NOT add DEFAULT_ADMIN_PASSWORD to Vercel (security risk)
